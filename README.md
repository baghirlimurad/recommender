# Song Recommender

This repository contains a music recommendation system for student led project using content-based filtering and data retrieval from Spotify's API.

## Getting Started

### Prerequisites

Make sure you have the following libraries installed:

- `fastapi`
- `pandas`
- `numpy`
- `spotipy`
- `sklearn`
- `python-dotenv`

### Installation

Clone the repository to your local machine:

```bash
git clone https://github.com/muradbaghirli/recommender.git
